import { VehicleSubmission } from '../models/vehicle-submission';

describe('VehicleSubmission', () => {
  it('should create an instance', () => {
    expect(new VehicleSubmission()).toBeTruthy();
  });
});
